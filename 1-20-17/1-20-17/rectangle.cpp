#include <iostream>
using namespace std;
int main() {
	int width;
	int length;
	int area;
	cout << "what is the width?" << endl;
	cin >> width;
	cout << "what is the length?" << endl;
	cin >> length;
	area = width*length;
	cout << "the area is " << area << endl;
}